# Example environment variables for deployment
# Copy to `.env` locally (do NOT commit secrets)

DJANGO_SECRET_KEY=replace-this-with-a-secure-secret
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=your-domain.onrender.com
# Example DATABASE_URL for Postgres (managed DB)
# DATABASE_URL=postgres://user:password@hostname:5432/dbname
